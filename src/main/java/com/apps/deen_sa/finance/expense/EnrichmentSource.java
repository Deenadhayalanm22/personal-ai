package com.apps.deen_sa.finance.expense;

/** Provenance boundary for optional semantics; more authoritative sources can be added without changing the merge API. */
public enum EnrichmentSource { EXPLICIT, UNKNOWN }
